---
name: brand-exporter
description: Export approved brand decisions as CSS variables and an agent-readable design package. Use after a brand identity passes approval and quality review.
---

# Brand Exporter

Read `references/workflow.md` for the complete procedure. Load only the additional references needed for the requested stage.

## Procedure

Use the imported workflow and keep state in the active manifest.
Export `motion/motion-tokens.css`, `motion/motion-tokens.ts`, `motion-guidelines.md`, `components/`, `components/README.md`, and `PACKAGE-MANIFEST.md` when those approved outputs exist; include Motion and Components entries in the package manifest.

## Output

Return the requested artifacts, provenance, unresolved gaps, and next action.

## Quality Checklist

Run the relevant validators before delivery; never promote unapproved artifacts.
