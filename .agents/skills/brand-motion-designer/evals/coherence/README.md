# Coherence Evals for brand-motion-designer

Given a set of element specs, the coherence review should flag token drift correctly.

## Fixtures
Each fixture has a pillar set, element specs, and an expected verdict.
