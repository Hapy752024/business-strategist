# Pillar Selection Evals for brand-motion-designer

Given a brand strategy brief, the skill should propose 2-4 pillars matching the heuristic in references/pillars.md.

## Fixtures
Each fixture has a brief and an expected pillar set (any 2+ overlap counts as a pass).
