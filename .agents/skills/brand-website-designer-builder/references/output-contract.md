# Output contract

The website deliverable contains editable Next.js source, approved optimized assets, `website-preferences.json`, `website-manifest.json`, screenshots, machine QA, human visual review, and an explicit list of unresolved content/legal/privacy/deployment gaps. Generated `.next` output is never treated as source.
