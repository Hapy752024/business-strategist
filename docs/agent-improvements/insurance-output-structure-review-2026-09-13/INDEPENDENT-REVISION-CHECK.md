# Independent check of Revision 2

13 September 2026. **Verdict: READY at plan level.** [Revision 2](IMPROVEMENT-PLAN.md) closes the six findings in the [original independent review](INDEPENDENT-ADVERSARIAL-REVIEW.md) and its two setup clarifications. No remaining implementation-blocking design inconsistency was identified. This verdict concerns a sufficiently concrete implementation plan; it does not establish implementation correctness, successful evaluation, execution authorization or approval to migrate the live insurance workspace.

This check read Revision 2 against the previously inspected local code and contracts. Line references below refer to Revision 2. The original findings remain valid descriptions of the earlier plan and existing implementation until changes are made and verified.

| Finding | Closure and remaining implementation obligation |
|---|---|
| **1. Selection, revisions, corrections and overrides — closed** | Lines **31–41** distinguish conflict-control `manifest_revision`, user-driven `selection_generation` and case `assessment_revision`. Checkpoints and consumers bind to the relevant authorities. Same-byte interpretation corrections, shared-source consumers, unrelated cases, stale handoffs and named prerequisite overrides now have explicit behavior. Selection and pending-state checks cannot be overridden. Lines **87–89** specify the meaningful A→B→A and correction sequences. This resolves the prior ambiguity without duplicating stage state in the registry. |
| **2. Exploratory appraisal and prerequisites — closed** | Lines **69–71** name one route and owner: `case-appraisal`, implemented as an `opportunity-risk-designer` mode. Its required initial research, permitted outputs, prohibition on commitment advancement and relationship to full specialist workflows are explicit. It does not use focused scope to evade gates. The router, hook, aliases and stage writer consume the same mode contract. The implementation must preserve ordinary evidence collection and later strategy prerequisites while making appraisal available before a passed pain gate. |
| **3. Publication, locking and recovery — closed** | Lines **75–83** expressly replace the assumption that existing atomic writes suffice. They define one project lock, revision checks under that lock, unique temporary/run paths, pending-before-current publication, a project completion marker, idempotent completion/rollback and protection against outside edits. Case stage writers avoid nested research locking. Readers refuse pending state; direct filesystem limitations are acknowledged. Line **89** provides targeted concurrency and interruption acceptance. This is proportionate to the promised multi-file history/current-output consistency. |
| **4. Shared reads, output paths and legacy behavior — closed** | Lines **65–67** distinguish explicit roots, case-local checkpoint assessments and read-only shared source bindings. They constrain case plus `--out-dir`, reject escaping destinations before creation and support nested discovery without a root research manifest. Lines **147–149** explicitly preserve ordinary legacy research continuation while making case-mode publication/execution migration-dependent. Legacy passes cannot become case passes implicitly. These are clear compatibility boundaries, not an automatic live migration. |
| **5. Semantic acceptance — closed** | Lines **111–113** require actual fresh-context host/model responses, saved artifacts, assertion-based human or separate review and reruns for failed assertions. The plan no longer treats structural fixture validation as research execution. Correction, ranking stability, cross-segment applicability, absent numbers and one-off/recurring distinctions are covered. Semantic acceptance expressly remains pending if no real responses are obtained. No additional evaluation platform is required. |
| **6. Bounded economics — closed** | Lines **119–131** specify transactional contribution/volume and a bounded recurring cohort/cash calculation, venture revenue rather than pass-through spend, acquisition units, rounding, cancellation exposure, founder-cost treatment and unresolved inputs. Opening cash and horizon are required for cash sufficiency. The input/result distinction and canonical input digest support recomputation and stale-result rejection. The synthetic contribution/volume example remains correct. The depth limit at **133** prevents expansion into a general finance framework. |
| **Explicit inputs and final writer ownership — closed** | Line **97** declares the dispatch context, one publisher for assembled case narratives, normal research-artifact ownership, economic-input ownership, calculated-result ownership and root-plan ownership. Revision checks prevent a stale section draft from silently replacing newer work. The setup clarification is incorporated into existing routing and publication, rather than introducing individual handoff schemas or additional agents. |
| **Recompute economics before publication — closed** | Line **127**, reinforced by **131**, makes inputs authoritative, results deterministic and narratives dependent on the same digest/results. Edited inputs cannot leave old numerical conclusions eligible for publication. This directly resolves the requested clarification. |

The author made two final clarifications after the initial revision check; both were independently reread and are **closed**:

- **Cash timing for both model types:** line **125** now expressly uses the same bounded monthly receipts/payments calculation for transactional and recurring cases. Transactional contribution alone cannot establish cash sufficiency. This resolves the wording ambiguity without a third model or financial framework.
- **Complete recovery bookkeeping:** line **79** now explicitly includes the project manifest and prior completion marker, affected case state/current outputs, canonical decision and timeline in the restoration inventory. Pending bookkeeping is excluded from self-digests. Whole-publication rollback is therefore stated directly rather than left implicit. The existing interruption fixtures remain the appropriate implementation proof.

The four-package scope remains justified. The added mode, revision fields, publication helper and economics helper each address an identified failure; the plan avoids extra skills, standing reviewers, dashboards, databases, host installers and parallel stage machines. Producer inventory and synthetic-first rollout remain prerequisites for default enablement. No broader architecture or test expansion is warranted by this revision check.

No web retrieval, provider access, installation, execution test, model evaluation or code change was performed for this follow-up. Only this new review file was written. The plan, original review findings and research artifacts were not edited. **READY means the plan is concrete enough to implement within its stated boundaries; every claimed behavior still needs the specified implementation evidence.**
