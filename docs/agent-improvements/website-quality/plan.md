# Website capability improvement plan

Scope: reusable agent setup only; preserve all pre-existing edits and projects. Extend existing website builder and Branding asset producer rather than install overlapping skill collections. User additions: Branding owns favicon creation; EU cookie banner must implement GDPR/ePrivacy-oriented consent.

1. Record dated primary-source research, including all nine unique GitHub repositories in the supplied tweet (retrieved through FxTwitter; direct X unavailable), plus Vercel skills, Next.js docs, Google Search and EU regulator guidance. Assess adopt/adapt/defer; no third-party code or installers executed.
2. Extend website-build routing vocabulary for maintenance, Next.js performance, technical SEO and GEO/LLM-EO, while keeping competitor analysis, app UI, marketing strategy and business gates distinct. Keep existing skill identity/catalog. Maintenance is a mode in the existing workflow, not a new orphan skill.
3. Add linked launch contract covering all 20 requested items, explicit page inventory, real rendered metadata/HTTP/browser evidence, semantic alt exceptions, performance budgets and lab/field separation, SEO/AI discoverability without ranking guarantees or mandatory llms.txt. Branding produces favicon bundle; website integrates and checks it. Consent reference specifies reject/accept/customize, default denial, withdrawal, persistence/versioning, vendor inventory, and browser network/storage verification including SPA transitions. Analytics implementation is prepared locally and activation follows existing authorization; launch remains blocked if unresolved. Never invent legal text facts/contact addresses.
4. Add a small canonical launch-check list and Python evidence validator. New website manifests initialize a versioned pending launch assessment; legacy manifests remain readable but production recording requires assessment. Each check requires pass plus nonempty evidence explanation; report includes exact release commit and tested HTTPS URL. This enforces completeness and release binding, not truth of browser tests. Existing release helper blocks production on missing/failed/stale assessment; preview and rollback remain usable. Keep schema and init consistent. No separate state store or deployment system.
5. Document operational maintenance: inventory, baseline, focused fixes, regression evidence, affected route checks, dependency upgrades with matched docs, redirects, rollback, periodic CWV/indexing/consent checks without automatically scheduling services.
6. Tests: pending initialization, all 20 mandatory entries, missing/failed/malformed checks, release mismatch and legacy production rejection without writes, preview/rollback compatibility, positive production record, route positive/negative boundaries. Run required setup/evals and quick skill validation. Real website quality and legal compliance require project-specific browser review; this task does not deploy a site.

Review protocol: fresh no-history adversarial plan reviewer, revise and repeat until no meaningful findings. Implement only after clean plan review. Fresh no-history implementation reviewers repeat until clean. Review only task changes against saved pre-edit snapshots, not unrelated dirty work.

Plan review 1 correction: production recording requires an exact match to the tested production URL and full 40-character source commit. Preview reports cannot stand in for production reports. After an authorized deployment, run production-specific metadata/robots/consent/analytics checks before recording production success. Record the tested deployment/configuration identifier; production recording must explicitly supply the same identifier, and configuration changes invalidate evidence even without source changes. This is an attestation boundary, not an independent deployment-provider lookup. Add URL/config mismatch no-write tests. Keep pending work visibly pending; never promote Preview-only evidence.

User testing addition: provide a runnable Playwright browser auditor (resolves Playwright from the target project's dev dependencies; no automatic install). Inputs: explicit target base URL and route inventory JSON. Output: machine JSON to stdout, nonzero on observed defects. Check rendered titles/descriptions/OG/alt, duplicate metadata, broken local links/resources, mobile/desktop overflow, above-fold and sticky CTA selectors on conversion pages, robots/sitemap/custom unknown URL response. Do not submit forms or click consent/marketing controls automatically. Document project-specific Playwright state/consent tests and Lighthouse CLI/CI performance budgets. Add deterministic local fixture tests for auditor logic and failure propagation where browser dependencies are available; otherwise label browser execution unverified. Browser output never auto-passes legal, brand or deployment attestations.

Explicit required checklist: custom 404; title on every page; description on every page; above-fold CTA; Branding favicon set integrated; robots.txt; sitemap.xml; Open Graph image; alt on every image (empty for decorative); mobile breakpoints; sticky mobile CTA; loading states; form error states; thank-you page; privacy policy; terms; GDPR/ePrivacy cookie banner; consent-controlled analytics installed; owner-verified real contact address; compressed images. Mandatory launch assessment also includes technical SEO/AI discoverability, measured performance and browser coverage.

User clarification during implementation: analytics itself is optional. Only analytics may have a not_requested disposition with user-choice evidence and verified absence of analytics. Undecided stays pending. If selected, PostHog is an optional provider with EU configuration/consent guidance; no service activated in this task.

Additional user scope: optional A/B experiments with PostHog exposure/conversion analysis; optional website campaign measurement using Google/GA4/GTM and Meta Pixel/CAPI, ideally linked to selected accounts. Extend existing experiment and website references and routing, not broad digital-marketing automation. Record consent-purpose separation, single event ownership/deduplication, server secret boundaries, account authorization and read-only ad-spend import versus conversion export. No live account connection in infrastructure work. These additions join the final fresh implementation review.

Robustness steering: document optional hybrid/server-side conversion delivery using existing backend/outbox infrastructure, idempotent events, bounded retries/dead-letter handling, consent at enqueue/dispatch and withdrawal cancellation, verified outcomes, no open proxy and provider receipt testing. This is reusable implementation guidance, not a live queue/account installation. Browser exposure and server outcomes remain distinct.
